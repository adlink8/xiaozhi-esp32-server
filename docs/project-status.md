# 项目状态文档（初稿）

> 说明：本文件根据 CLAUDE.md、README.md 以及当前代码结构自动生成，主要目的是给你一个“项目状态总览”的模板，方便你后续按实际进度维护。

---

## 1. 基本信息

- **项目名称**：xiaozhi-esp32-server
- **主要模块**：
  - `main/xiaozhi-server`：Python AI 引擎（WebSocket + ASR/TTS/LLM）
  - `main/manager-api`：Java Spring Boot 管理后端
  - `main/manager-web`：Vue.js Web 管理前端
  - `main/manager-mobile`：uni-app 移动端
- **部署方式**：
  - Docker 最简化安装 / 全模块安装（参考 `docs/Deployment*.md`）
  - 源码模式运行（参考 CLAUDE.md 中“本地运行指南”）

---

## 2. 当前模块状态概览（请按实际情况更新）

> 下面是一个建议的状态标记方式：`✅ 已可用` / `🟡 可用但待优化` / `🚧 开发中` / `❌ 未启用`。

| 模块 | 位置 | 状态 | 说明 |
|------|------|------|------|
| Python AI 引擎 | `main/xiaozhi-server` | 🟡（示例） | 基本语音链路已具备，部分 ASR/LLM/TTS 组合需按环境调试 |
| Java 管理后端 | `main/manager-api` | 🟡（示例） | 接口已经成型，具体配置和权限策略视你实际部署而定 |
| Web 管理前端 | `main/manager-web` | 🟡（示例） | 智控台页面可用，样式与功能后续可继续优化 |
| 移动端（H5/uni-app） | `main/manager-mobile` | 🚧（示例） | 基本结构就绪，存在 TODO 需要按移动端场景完善 |

> 请根据你自己的感受，将上述状态标记改为更符合当前真实情况的值，并在“说明”中写具体点。

---

## 3. 已完成的关键事项（示例）

> 这里建议只记录“对你有意义的”里程碑，而不是所有 commit。

- ✅ 核心架构搭建：MQTT+UDP 网关、WebSocket、HTTP 服务整体打通
- ✅ 基础 AI 能力接入：至少一种 ASR、LLM、TTS 组合在本地环境可用
- ✅ 智控台 Web 管理界面建立（manager-web）
- ✅ RAGFlow、PowerMem 等外部能力基础集成文档编写（docs/ 中的各集成文档）

> 请根据你的实际历程补充，比如“完成硬件烧录和设备接入”、“完成某个 demo 场景联调”等。

---

## 4. 进行中的工作

> 建议和 `docs/todo.md` 保持联动，这里可以只列出比较“粗粒度”的进行中事项。

- 🟡 优化本地开发体验（config.yaml、日志、调试脚本等）
- 🟡 完善管理端功能（用户管理、多设备配置、更多可视化）
- 🟡 整理文档体系（部署、集成、项目管理三件套）

> 请把你现在真正“在做的事情”写进来，并简要说明目标。

---

## 5. 待办事项总览（与 docs/todo.md 对齐）

> 这里给一个聚合视图，详细任务拆解放在 `docs/todo.md`。

- 高优先级：
  - 保证一条稳定的语音交互链路（设备 → ASR → LLM → TTS → 设备）在你的目标环境下无明显问题
  - 完成基础安全与配置检查（密钥管理、端口暴露、Docker 与源码模式的一致性）

- 中优先级：
  - 完善管理端的多设备视图与操作
  - 梳理和优化集成文档，在 README 中挂好入口

- 低优先级：
  - 清理代码中的 TODO/FIXME
  - 统一前后端代码风格与注释

---

## 6. 版本与环境信息

> 此处建议你按真实运行环境补充：

- 当前使用的后端镜像 / 源码版本：
  - Git 分支：`new-files-branch`（示例）
  - 最近重要提交：`feat: add diagnostic skills index and hardware connection configuration` 等
- 运行环境：
  - 系统：WSL2 + Ubuntu + Docker（参考 CLAUDE.md）
  - 部署方式：Docker 全模块 / Docker 最简化 / 源码模式（请勾选）
- 设备环境：
  - ESP32 型号、固件版本、部署 IP 等（可直接复用 CLAUDE.md 中的表格）

---

## 7. 风险与问题记录

> 将运行过程遇到的“已知问题”记录在这里，方便后续排查和优化。

- 示例：公网暴露后的安全性与限流策略尚未系统评估
- 示例：部分云服务（ASR/LLM/TTS）在网络不佳时有明显延迟
- 示例：移动端界面的某些交互存在体验问题

---

## 8. 更新记录

| 日期 | 修改人 | 变更内容 |
|------|--------|----------|
| 2026-03-16 | （自动生成，待你更新） | 创建初稿：模块概览 + 状态表 + 待办视图 |

> 建议每次重要调整（例如阶段完成、环境迁移、模型更换）后，在此处追加一行说明。